[project]
name = "divi-spin-dynamics-demo"
version = "0.1.0"
description = "Divi Spin Dynamics (TFIM) demo — data-driven Streamlit app."
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "streamlit>=1.60.0",
    "qoro-divi[jupyter]==0.13.0",
    "pyyaml",
    "matplotlib",
    "numpy",
]

[tool.uv]
package = false
